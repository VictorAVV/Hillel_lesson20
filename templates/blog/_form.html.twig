{{ form_start(form) }}
    {{ form_widget(form, {'attr': {'class': 'form-group'}}) }}
    <button class="btn btn-primary mr-2">{{ button_label|default('Сохранить') }}</button>
    <a class="btn btn-secondary" href="{{ path('blog') }}">Отмена</a>
{{ form_end(form) }}
