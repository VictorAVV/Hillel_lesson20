{% extends 'base.html.twig' %}

{% block title %}MVC-блог на Symfony / {{ title }}{% endblock %}

{% block body %}
<div class="jumbotron text-center">
	<h3>{{ title }}</h3>
</div>

<div class="container">
    <div class="container my-3 px-0">
        {{ include('blog/_form.html.twig') }}
    </div>
    <a class="btn btn-secondary" href="{{ path('blog') }}">Вернуться к списку статей</a>
</div>
{% endblock %}